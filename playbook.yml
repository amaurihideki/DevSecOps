---
- name: Configurando um script
  hosts: prod
  vars:
  tasks:
  - name: verificando usuario logado
    command: whoami

  - name: verificando se o vim esta instalado
    dnf: name=vim state=present
    when: ansible_distribution == "Fedora"

  - name: garantindo que o ssh esta rodando
    service: name=httpd state=started

  - name: garantindo que o ssh esta rodando
    service: name=mysql state=started

  - name: garantindo que o ssh esta rodando
    service: name=php state=started

